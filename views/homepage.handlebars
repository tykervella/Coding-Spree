{{#each posts as |post| }}
<div class="newPost">
  <div class="postTitle">
    <h2>{{post.title}}</h2>
  </div>
  <div class="postContent">
    <p>{{post.content}}</p>
    <p> Post created on {{formatDate post.createdAt}}</p>
  </div>
</div>
{{/each}}
