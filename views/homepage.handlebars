{{#each posts as |post| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{post.title}}</h2>
  </div>
  <div class="col-md-6">
    <p>{{post.content}}</p>
    <p> Post created on {{formatDate post.createdAt}}</p>
  </div>
</div>
{{/each}}
